// import { createSlice } from '@reduxjs/toolkit';

// const initialState = {
//   loginStatus: [],
// };

// export const loginSlice = createSlice({
//   name: 'loginModal',
//   initialState,
//   reducers: {
//     showLoginModal: (state, action) => {
//         state.loginStatus = [...state.loginStatus, action.payload] as any;
//     },
//     closeModal: (state, action) => {
//       state.loginStatus = initialState.loginStatus;
//     },
//   },
// });

// export const { showLoginModal, closeModal } = loginSlice.actions;

// export const selectloginStatus = (state: any) => state.login.loginStatus;

// export const setModal = (state: any) =>
//   state.login.loginStatus.reduce((openModal: any) => openModal, '');

// const loginReducer = loginSlice.reducer;

// export default loginReducer;

export default function t0() {}
